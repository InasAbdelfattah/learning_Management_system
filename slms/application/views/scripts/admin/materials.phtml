<!-- Page Heading -->
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Materials Control
        </h1>
        <ol class="breadcrumb">
            <li>
                <i class="fa fa-dashboard"></i>  <a href="<?php echo $this->baseUrl() . '/admin' ?>">Dashboard</a>
            </li>
            <li class="active">
                <i class="fa fa-edit"></i> Material Control
            </li>
        </ol>
    </div>
</div>
<!-- /.row -->
<div class="col-lg-12">
    <a href="<?php echo $this->baseUrl() . '/admin/addmaterial' ?>" type="submit" class="btn btn-md btn-success pull-right" />Add New Material</a>
<h2>Materials Table</h2>
<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Material</th>
                <th>Category</th>
                <th>Course</th>
                <th>Image</th>
                <th>Description</th>
                <th>Add Time</th>
                <th>Type</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>

            <?php foreach ($this->materials as $material) { ?>
                <tr>
                    <td><a href="<?php echo $this->baseUrl().'/materials_upload_folder/'. $material['material_name'] ?>"><?php echo $material['material_name'] ?></a></td>
                    <td><?php echo $material['course'] ?></td>
                    <td><?php echo $material['course'] ?></td>
                    <td><img src='<?php echo $this->baseUrl().'/img/materials_images_folder/' . $material['image'] ?>' width='30px' height="20px"></td>
                    <td><?php echo $material['descib'] ?></td>
                    <td><?php echo $material['created_at'] ?></td>
                    <td><?php echo $material['type'] ?></td>

                    <td>
                        <input type="submit" class="btn btn-md btn-danger" name="delete" value="Delete"/>
                        <input type="submit" class="btn btn-md btn-primary" name="active" value="Active"/>
                    </td>
                </tr>
            <?php } ?>

        </tbody>
    </table>
</div>
</div>
<div class="col-lg-7">
    <a href="<?php echo $this->baseUrl() . '/admin' ?>" type="submit" class="btn btn-md btn-success pull-right" />Add New Material Type</a>
<h2>Materials Types Table</h2>
<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Material Type</th>
                <th>Created At</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->materialsTypes as $materialsTypes) { ?>
                <tr>
                    <td><?php echo $materialsTypes['material_name'] ?></td>
                    <td><?php echo $materialsTypes['created_at'] ?></td>
                    <td>
                        <input type="submit" class="btn btn-md btn-danger" name="delete" value="Delete"/>
                        <input type="submit" class="btn btn-md btn-primary" name="active" value="Active"/>
                    </td>
                </tr>
            <?php } ?>
        </tbody>
    </table>
</div>
</div>

